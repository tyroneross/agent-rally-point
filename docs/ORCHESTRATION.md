<!--
SPDX-FileCopyrightText: 2025-2026 Tyrone Ross, Jr <46267523+tyroneross@users.noreply.github.com>
SPDX-License-Identifier: Apache-2.0
-->

# Orchestration board — agent-rally-point multi-agent work

> **Lead orchestrator:** `claude_code:lead` (this session — scoped id; bare `claude_code` = the PR46/L4 terminal). **Codex-side coordinator:** `codex:dynwf-coordinator` (delegated, acknowledges lead).
> **Coordination substrate:** the `rally` CLI (this repo). **Source of truth for status:** this file + `rally room`.
> **Protocol:** every lane `rally say claim` + `rally check before-write` before editing its owned paths; commit at each
> stop point; the lead syncs this board as commits/facts land. Updated as a binding `decision` in the room.

This board is **dogfoodable**: it is the human-readable projection of the rally facts. A fresh agent (Claude or Codex)
reads this + `rally room --json` and knows what is owned, in flight, landed, and next — without the user relaying it.

## ▶ Resume here — lead handoff snapshot

**Lead:** `claude_code:lead`, from its own worktree `~/dev/git-folder/arp-lead` (branch `lead-main` → origin/main). A fresh lead resumes from **this file + `rally room --json`** — no user relay needed.

- **L4 split (source-verified 2026-05-29):** the **L4-scoped** merge (audit-folds + `1ea786a` seq-cast/scope-match/score-clamp hardening) **IS on main** (`--merged main` confirms, 0 unmerged commits). The **PR46 *feature* surface** (`--produces`/`--depends`, transitive receipts, `check ci`) is **NOT on main** → now **B13** (deferred, available for pickup). Stale `next.rs`/`store.rs` claims → Claude-L4 to release (handoff sent; lead deconflicts if idle).
- **In flight:** nothing actively executing. **Waiting on peers:** Claude-L4 to release stale `next.rs`/`store.rs` claims (handoff sent); a Codex squad to pick up **B13** / **B10–B12**.
- **Open backlog:** **B10, B11, B12, B13** (B10–B12 **unblocked** — L4-scoped landed; coordinate w/ B13 on shared `check/store/next.rs`), **B2/L5**, **L2**. **B1–B9 + B14 + B15 all ✅ closed + verified.**
- **Durable outputs this session:** **Rally Flow** (the `dynamic-workflows/` module: L1+L6 — protocol, `workstream-lint`, `route.mjs`, `workstream-status` resume, host skills; simplified + deduped in B15); doctrine `COORDINATION.md`; `MODEL-TIERS.md` (+ A/B verdict); `RALLY-AUTOMATION-PROPOSALS.md`; assessment `docs/assessment-2026-05-29-rallyflow-architecture.md` (B14/B15 done); doc-hygiene sweep (B14). nimbalyst adoption candidates kept as summary only (source packet deconflicted — separate repo, recoverable from `d6c9af1`).
- **Next lead actions:** (1) on a Codex B13 pickup → audit post-hoc, then flip B2/B3/L5; (2) keep contracts **host-relative** (model tier + execution substrate); (3) **open item for user:** rally event-ledger is local-only (`.rally/` gitignored) — offered options A (export to committed JSONL, recommended) / B / C; awaiting pick. Coordination is host-relative + charter-safe (rally facilitates, never executes/decides).

## Squad roster (2 Claude + 3 Codex squads)

A **squad** = one terminal (an agent team: one agent or many inside it). **Distinct squad id + model tier**, enforced at `rally enter` — bare/shared ids retired. *(Rally-side enforcement = B11; lead enforces the convention until then.)*

| Squad | id | Host · model tier | Role / lane |
|-------|----|--------------------|-------------|
| **Claude-Lead** | `claude_code:lead` | Claude · **Frontier** (Opus 4.8) | lead orchestration, board, Rally Flow (L1/L6), audit. No `crates/**` writes. |
| **Claude-L4** | `claude_code:l4` | Claude · **Frontier** (Opus) | L4/PR46 (next.rs, store.rs) — has merge authority |
| **Codex-Coord** | `codex:dynwf-coordinator` | Codex · **Executing** (self-reported gpt-5.4-mini, seq283) | coordination, review, assessments |
| **Codex-Build** | `codex:dynamic-scale-01` | Codex · **Executing** (model squad-declared — pending B11) | `crates/**` fixes (B4–B9), managed-session (L2) |
| **Codex-Builder** | `codex-gpt-5.5-builder-01` | Codex · **Executing** (**GPT-5.5**, in id) | the GPT-5.5 squad the user is driving |

### Squad-id convention (user-set): `host-llm-role-number`

`codex-gpt-5.5-builder-01` = **host** (`codex`) · **LLM** (`gpt-5.5`) · **role** (`builder`) · **number** (`01`). The id is **self-declaring** — the model lives *in the id*, so the lead never infers it (this is what closes the model-attribution gap; it makes B11's model-registration redundant — tier/uniqueness is all B11 still needs to enforce). **One real tradeoff:** the model token is mutable, the id should be stable — so a model upgrade (5.5→5.6) = **re-enter under the new id + release old claims** (cheap, rare; a different model is arguably a different squad capability). **Migrate on re-entry, not by force-rename** — live squads keep their current id until they next `enter` (force-renaming orphans in-flight claims/handoffs). Roster shows mixed schemes during transition.

Retired: bare `codex`, bare `claude_code`, transient `codex:optimizer`. **One squad = one id.**

## Lanes

| Lane | Owner (rally tool) | Owns | Status | Stop-point commits | Next checkpoint |
|------|--------------------|------|--------|--------------------|-----------------|
| **L1 · dynamic-workflows module** | `claude_code:lead` | `dynamic-workflows/**` | ✅ landed | `6d76780`, `85a089a`, `d52a184` | discovery wiring (deferred) |
| **L2 · managed-session scale hardening** | `codex` / `codex:dynwf-coordinator` | `crates/rally-cli/src/{cli,backends,lib}.rs` (managed-session paths) | ✅ landed locally | `f852960`, `63f7a66`, `f6bab07` | keep installed CLI aligned before new dogfood |
| **L3 · bpaf run/inject/capture/stop --help panic** | `codex:dynamic-scale-01` | `crates/rally-cli/src/cli.rs` (parsers) | ✅ fixed (lead-verified) | `9332915`, `b056855` (local; push pending) | — `rally run --help` exits 0 ✓ |
| **L3b · room projection: clear resolved risks** | `codex:dynamic-scale-01` | `crates/rally-cli/src/store.rs` (projection) | ✅ fixed locally | `0d41d4a` | resolved risks drop from `current_risks` |
| **L4 · PR46 port** (contract-claims + receipts + CI gate) | `claude_code` (Claude #2) | `crates/rally-cli/src/{next,store}.rs` + PR46 additions; **folds** B7-store (seq cast) + B8-next (scope-match/confidence). check.rs via Codex B4 (rebase). | ✅ **L4-scoped landed** (`1ea786a` on main, `--merged` confirmed); **PR46 feature surface deferred → B13** (never reached main) | `1ea786a` | B13 pickup → unblocks B2/B3/L5 |
| **L5 · observation seam** (Plan B: DAG / wake-due / heartbeat) | unassigned | TBD | ⏸ deferred | — | gated on L4 lineage landing |
| **L6 · durable fan-out + resume** (dynamic-workflows) | `claude_code:lead` | `dynamic-workflows/**` | ✅ landed · `e1589dd` | — | route.mjs (parallel/pipeline/budget, host-neutral + onError/abort) + workstream-status.mjs resume; 35 tests |

## Dogfood linkages
- **L2 → L1**: managed-session auto-numbering + parallel-launch id reservation is the substrate L1's **Tier-2 cross-host**
  fan-out (`rally run` / `rally inject`) rides on. L1's skills document that path; L2 makes it scale.
- **L3 → L1**: the bpaf `--help` panic affects exactly the `rally run`/`inject` surface L1's skills reference. L1 already
  corrected its documented syntax (`d52a184`); L3 fixes the CLI itself. **Pattern flagged** (positional-not-rightmost) so
  L4 can audit its own new parsers (`check ci`, `--produces`/`--depends`) for the same defect.
- **L4 → L5**: the seam's DAG + wake-due lean on PR46's `produces`/`depends` lineage + receipt lifecycle. A before B.

## Stop-point / commit policy
1. Each lane commits at a **verifiable stop point** (its F-criteria green), not mid-edit.
2. The lead syncs this board + re-commits it whenever a lane lands a stop-point commit or posts a room fact that changes
   status. Commit subject prefix: `docs(orchestration): sync board — <what changed>`.
3. Branch hygiene: lanes collapse onto `main`; no leftover branches/worktrees. One folder, one branch.
4. Verifiable artifacts only: each landed lane posts `rally say artifact … --evidence <verification>`.
5. **Board ownership — lead edits only.** Only `claude_code:lead` edits this file. Other agents communicate status via **rally facts** (`claim` / `artifact` / `decision` / `resolve`); the lead projects them onto this board. This prevents board merge conflicts (a set was briefly committed and resolved at `3ae7ec8`). If you are not the lead and need a status change reflected, post the fact — do not edit this file.
6. **Canonical claim paths.** Claims MUST use the repo-relative path `crates/rally-cli/src/<file>` (not `src/<file>`). A `src/lib.rs` claim did **not** auto-detect against a `crates/rally-cli/src/lib.rs` claim (the scope-matching bug tracked in B7/B8), causing a real lib.rs double-claim that the lead deconflicted manually. Until B7/B8 land, the gate cannot be fully trusted for overlap — use identical canonical paths and check the roster's owns column before claiming.

## Delegation & backlog protocol

A task the user gives the lead can be resolved three ways — the lead chooses:

1. **Execute** directly (lead owns the lane).
2. **Delegate** to a named agent — `rally say handoff --tool claude_code:lead --target <tool> --subject "<task>" --summary "<owns + validation + deps>"`. The target acts and posts an artifact; the receipt closes the handoff.
3. **Backlog** — add a row to the Backlog below for *any free agent* to pick up.

**Pickup protocol (any free agent, Claude or Codex):** read this board + `rally room` → choose an `open` Backlog item whose `depends_on` are ✅ → `rally say claim --tool <you> --subject "<item id>" --path <owns>` → `rally check before-write --path <owns> --strict` → do the work → `rally say artifact --tool <you> --uri <path> --evidence "<validation>"` → post a `resolve`/`artifact`; the lead flips the row to `done · <you>`. **Never start an item whose deps are unmet** — the lead flips a row to `open` only once its dependencies have landed.

## Backlog (free agents may claim any `open` item whose deps are met)

| ID | Item | Context | depends_on | owns (MECE) | suggested validation | status |
|----|------|---------|-----------|-------------|----------------------|--------|
| **B1** | Wire dynamic-workflows skills into host discovery | Surface the claude/codex skills via host skill discovery; point `.codex-plugin/skills/dynamic-workflows` at the canonical module skill | none (L1 ✅) | `dynamic-workflows/skills/**`, `.codex-plugin/skills/dynamic-workflows/*` (coordinate w/ codex) | skill resolves in host; `npm test` still green | ✅ done · `2ca7d33` (symlink) |
| **B2** | Observation seam (= L5) | Plan B: orchestrators emit `handoff/artifact/decision/standby/wake`; rally derives a DAG + wake-due; **never executes** | **B13** (PR46 `produces`/`depends` + receipts on main) | `crates/rally-cli/src/{dag.rs(new),next.rs}`, `examples/` | `rally dag --run <fixture>` reconstructs fan-out; charter assertion (no exec path) | blocked (dep B13) |
| **B3** | Audit PR46 parsers for the L3 bpaf defect | `check ci` + `--produces`/`--depends` parsers may repeat the positional-not-rightmost panic L3 had (help-sweep covered existing subcommands; confirm L4 additions) | L4 verified ✅ | `crates/rally-cli/src/cli.rs` (review only, on claude2 branch) | `rally <new-subcommand> --help` exits 0 | ✅ clean · codex (seq298: no new parser surface) |
| **B4** | `check.rs` boundary-gate integrity (HIGH, security) | `rally check before-write` is bypassable: `allow` true in warn mode w/ stop findings (:70); missing `--path` skips all checks (:104-115); `--tool` omitted → `"unknown"` suppresses conflicts (:120-122). Undermines the "agents don't conflict" guarantee. Detail: `docs/audit-2026-05-29-rally-cli.md`. | none (coordinate w/ L4 — shares check.rs) | `crates/rally-cli/src/check.rs` | tests: stop⇒allow=false in warn; missing-path still evaluates global facts; unknown-tool rejected | ✅ done · `12468f9` |
| **B5** | `lib.rs` NUL-byte panic + session-cleanup (HIGH) | `shell_quote` `.expect` panics on NUL (:950); error-recovery discards stopped-session write (:337) → session stuck active; + lib mediums (envelope swallow, repo_root fallback) | none | `crates/rally-cli/src/lib.rs` | NUL input → handled error not panic; `cargo test` | ✅ done · `e7b87aa`,`3bdc040` |
| **B6** | `backends.rs` NUL-byte panic + parse fallbacks (HIGH) | `shell_words` `.expect` panics on NUL (:497); cmux target falls back to arbitrary first line (:388); herdr focused-tab not excluded (:341) | none | `crates/rally-cli/src/backends.rs` | NUL → error; cmux parse failure surfaces; `cargo test` | ✅ done · `6ec1042` |
| **B7** | `discovery.rs` data-integrity (HIGH) | index clobbered on read-error (discovery:95 — destroys recorded rooms) + TOCTOU/context-swaps. **store.rs seq-cast moved to L4** (Claude #2 owns the file). | none | `crates/rally-cli/src/discovery.rs` | corrupt-index doesn't wipe; `cargo test` | ✅ done · `c72328f`,`d39aab7` |
| **B8** | `cli.rs`/`output.rs` consistency (MED/LOW) | `bounded_i64` silent clamp (cli:514); aliased-flag drop (cli:526); dead arms; infallible-serialize fallback (output:31). **next.rs items moved to L4.** | none | `crates/rally-cli/src/{cli,output}.rs` | findings addressed; `cargo test` | ✅ done · `4e1a329` |
| **B9** | `lib.rs` residual findings (MED/LOW, from A/B) | advanced-flag hardcoded true (:125); envelope swallows serde err → empty {} (:1203); capture empty-text (:726); wait_for_resolution busy-poll (:931). All A/B-verified. | none | `crates/rally-cli/src/lib.rs` | findings addressed; `cargo test` | ✅ done · `ea6aee9` |
| **B10** | Canonical-path matching in `claim`/`check` (**rank-1 linchpin** — see `RALLY-AUTOMATION-PROPOSALS.md`) | the gate that let the lib.rs collision through; 4 other capabilities depend on it | none (L4-scoped landed) | `crates/rally-cli/src/{check,store,discovery}.rs` (path logic) | `check before-write` detects cross-path-form collisions; `cargo test` | open · coordinate w/ B13 on check/store.rs |
| **B11** | **Squad identity enforcement** (rank 2) + structured assignment contract (rank 10) | `rally enter` **rejects a duplicate squad id** + auto-asserts first squad as lead; `rally room` exposes `squads[]` + `room.lead`; `rally next` renders the 11-field contract. **Model now lives in the id** (`host-llm-role-number`) so model-registration is redundant — B11 enforces id-uniqueness + tier + lead-assert + contract only. | none (L4-scoped landed) | `crates/rally-cli/src/{lib,next}.rs` (enter/next/room) | duplicate squad id rejected; `squads[]` shows id+tier; typed contract carried | open · coordinate w/ B13 on next.rs |
| **B12** | `rally board` projection (rank 5) + artifact-significance filter (rank 3) | auto-derive the board from facts; tag idle-checkpoint noise so the stream is signal-only. **+ fold:** `RoomSnapshot::stale_facts` (store.rs:472) is serialized but always `Vec::new()` → implement stale-fact detection here or remove the field (assessment `wor9rkkhp`). | none (L4-scoped landed) | `crates/rally-cli/src/{lib,store}.rs` (projection) | `rally board` reproduces ORCHESTRATION lanes/backlog; `--artifacts signal`; `stale_facts` populated or removed | open · coordinate w/ B13 on `store.rs` |
| **B13** | **PR46 feature surface** (deferred from L4) | `--produces`/`--depends` predictive contract-claims, transitive **handoff receipts**, `check ci` gate. The L4 branch landed only the *audit-folds*; this feature never reached main (source-verified: no markers). Unblocks B2/L5. **+ fold:** wire/relocate orphaned `docs/schemas/agent-rally.session-backend.v1.json` (assessment `wor9rkkhp`). | none | `crates/rally-cli/src/{store,check,cli,next}.rs` | `rally claim --produces/--depends` round-trips; 2-hop receipt regression; `check ci --strict` exits 0 clean / 4 on stale-base+blocker | **open** (deferred, available) |
| **B14** | **Doc-citation sweep** (INTEGRITY, from `wor9rkkhp`) | Cleared stale/dangling doc refs: `manifest.toml` (cli_entry=rally), `RALLY.md` (+5 subcommands), `README.md` (+locate/recent), `WAKE_COORDINATION_PLAN.md` + `pi-dynamic-assessment-handoff.md` (citations), `PLAN-*.md`, `scripts/rally_wake.py` (doc). Detail: `docs/assessment-2026-05-29-rallyflow-architecture.md`. | none | top-level docs + `examples/manifest.toml` + `scripts/` (**NO `crates/**`**) | each cited path resolves/aspirational; `cargo build` unaffected | ✅ done · `a0140a4` · lead-audited PASS |
| **B15** | **dynamic-workflows simplifications** (6 simplify + 2 integrity, from `wor9rkkhp`) | De-dup `createLimiter` test, drop route.mjs re-export, inline 1-elem determinism loop, dedup claude/codex SKILL.md → `skills/SHARED.md`, document `norm()` divergence, **kept** `extractRoom raw.data.room` (verified real shape) + tests, drop README "35 tests", B1 skills cross-ref added. | none (lead lane) | `dynamic-workflows/**` | `npm test` 37/37; lint still rejects determinism + missing-MECE | ✅ done · `641ca25` · lead-audited PASS |

## Adoption candidates — from external Nimbalyst Rally Flow dogfood

A 4-facet Rally Flow fan-out assessed the separate Nimbalyst codebase for patterns worth lifting. The source assessment packet belongs outside this `agent-rally-point` repo; this board retains only the Rally-relevant candidate summary. Status **`proposed`** — *adoption is a product decision (user's call)*, not auto-claimable. **Astronomer UI items (5,6,10–13,16) route to the agent-astronomer repo, NOT here.** Rally-relevant items, with overlap to existing backlog called out:

| ID | Candidate (rally only) | Overlap / note | Tier |
|----|------------------------|----------------|------|
| **C8** | Persisted file-scoped `claim` facts with `link_type` (`claim\|edit\|read`) → `rally room` projects who-owns/touched | **strengthens B10** (adds persistence + link_type to canonical-path matching) | M |
| **C9** | `resume_hint` blob on facts (last file, next sub-step, open blocker) | complements `workstream-status.mjs` resume + **B11** contract | M |
| **C14** | `worktree: true` descriptor field → host provisions a worktree, emits path as artifact URI | extends **Rally Flow** descriptor (`dynamic-workflows/`) | M |
| **C15** | Kanban projection: `workstream-status.mjs` emits static HTML from `rally room --json` | **overlaps B12** (board projection) | L |
| **C-docs** | Area-gated `CLAUDE.md` routing table · `agent-mistakes.md` · `FEATURE_INVENTORY.md` (every subcommand) · per-subsystem invariant docs · ASCII/Mermaid pipeline diagrams | legibility; item 3 would've caught the drift early | S |
| **C17** | nimbalyst-as-rally-host extension (rally-backed MCP claim/check/artifact in nimbalyst sessions) | larger bet | L |

*Process finding from the dogfood: it independently re-surfaced the RALLY.md-vs-binary drift — **already fixed** this session (`cargo install --path crates/rally-cli` realigned the stale `~/.cargo/bin/rally`). Corroborates the earlier finding.*

## Live status log (newest first)
- **2026-05-29 (sync 31)** — **Nimbalyst assessment deconflicted** (user catch): removed the source packet from this repo because Nimbalyst is a separate repo. Kept only the Rally-relevant adoption-candidate summary above; source assessment artifacts should live with the Nimbalyst/cross-repo assessment surface, not under `agent-rally-point/docs/`.
- **2026-05-29 (sync 30)** — **B14 + B15 landed via lead-dispatched build-loop** (`a0140a4` doc-sweep, `641ca25` module-simplify), **lead-audited PASS** (re-ran gates myself: npm 37/37, lint exit 0, cargo build clean, 0 `crates/**` files). build-loop made 2 ground-truth corrections to the assessment: `rally_wake.py` not a true orphan (has inbound refs — fixed discoverability, not deprecated); `extractRoom raw.data.room` IS the real `rally room --json` shape (kept + tested, not removed). `codex:dynwf-coordinator` (seq391) caught a kind-vs-lane taxonomy mismatch → reconciled + documented (`8d30775`). Claim released.
- **2026-05-29 (sync 29)** — **Assessment `wor9rkkhp` landed + aggregated.** 4-agent read-only fan-out → 18 findings (12 integrity + 6 simplify, 6 high). Preserved to `docs/assessment-2026-05-29-rallyflow-architecture.md`. Ownership-split: lead-owned cluster → **B14** (doc-citation sweep) + **B15** (dynamic-workflows simplifications), both **lead-executing via build-loop** (lead lane, SAFE, no `crates/**`). Crate items folded → **B12** (`stale_facts` always-0) + **B13** (orphaned session-backend schema) — Codex lane, not lead-touched. B12 stale_facts + B13 schema notes added.
- **2026-05-29 (sync 28)** — Aggregated the **Nimbalyst Rally Flow dogfood** as a cross-repo intake. Folded rally-relevant items into **Adoption candidates** (C8/C9/C14/C15/C-docs/C17), `proposed` (adoption = user decision); astronomer UI items routed to that repo. Its process finding independently re-caught the now-fixed binary drift. Source packet later removed from this repo in sync 31 because Nimbalyst is a separate repo.
- **2026-05-29 (sync 27)** — **L4 split + B13 + squad-id convention.** Source-verified Codex-Coord's post-merge handoff: **L4-scoped IS on main** (`1ea786a`, `--merged` confirms, 0 unmerged) but the **PR46 feature surface never landed** (no `produces`/`depends`/`receipts`/`check-ci` markers) → new **B13** (deferred, open). B2 re-pointed to B13; **B10–B12 unblocked** (old L4 file-contention gone) but coordinate w/ B13 on shared `check/store/next.rs`. Handed Claude-L4 the stale-claim release; answered Coord (charter-correct read-only flag). **Squad-id convention adopted (user):** `host-llm-role-number` (`codex-gpt-5.5-builder-01`) — model in the id closes the inference gap; migrate-on-reentry; B11 model-registration now redundant.
- **2026-05-29 (sync 26)** — **Model attribution corrected** (user catch): I'd tagged BOTH Codex squads "gpt-5.4-mini" — an over-generalization from `codex:dynwf-coordinator`'s one self-report (seq283) + a stale March-2026 search. The optimizer/coded squad is on **GPT-5.5**. RULE reinforced: **model is squad-declared at `enter` (B11), not lead-inferred**; tier (Executing) is the stable abstraction, the GPT-5.x version is mutable + per-squad. Roster + MODEL-TIERS corrected.
- **2026-05-29 (sync 25)** — **Squad model adopted** (per user): a **squad = one terminal** (agent team — one agent like a single GPT-5.5/Opus, or many like a build-loop orchestrator+subagents). **N terminals = N squads (1:1)**; rally coordinates the squad, internal agents hidden behind the squad id. **Rally must enforce a distinct squad id + register model tier at `enter`** (→ sharpened B11). Roster reframed to **4 squads**: Claude-Lead [Frontier/Opus], Claude-L4 [Frontier/Opus], Codex-Coord [Executing/gpt-5.4-mini], Codex-Build [Executing/gpt-5.4-mini]. Codified in COORDINATION.md "## Squads".
- **2026-05-29 (sync 24)** — **Codex id collision fixed** (per user): two Codex terminals shared an id (ambiguous, like bare `claude_code` earlier). RULE posted: one terminal = ONE distinct stable scoped id; **bare `codex` retired**. Assignment: `codex:dynwf-coordinator` (coordinator) / `codex:dynamic-scale-01` (fixer) / `codex:optimizer` (was bare `codex`). Each re-enters under its single id. **Drift CORRECTION** (prior sync): the claim was REAL — `~/.cargo/bin/rally` was the stale attuned-line build; **rebuilt to the lean line** (both binaries now expose enter/say/room; the flagging agent was right, my dismissal was the error). **Codex tests audited TRUE**: independently re-ran `cargo test -p rally-cli` on main — green.
- **2026-05-29 (sync 23)** — **Merge documentation required** (per user): every merge to `main` appends a context entry to **`docs/MERGES.md`** (what/why/evidence/audit/reversibility — bookmark-style durable context) + posts a merge artifact; lead audits it. COORDINATION field 4 updated; MERGES.md seeded + L4 placeholdered as the first formal entry. **Drift claim resolved = FALSE POSITIVE**: a frontier agent misread `rally --help` "Fact kinds:" (claim/artifact/decision are `say <kind>` kinds, not commands) — actual surface MATCHES RALLY.md; do not rewrite it (broadcast as binding decision, esp. with merge authority live). Real adjacent findings flagged: 2 rally binaries on PATH (~/.local + ~/.cargo — stale risk) + `--version` unsupported.
- **2026-05-29 (sync 22)** — **Authority pushed down** (per user): commit + **MERGE** delegated to frontier agents — **Claude #2 merges L4 itself** (don't wait on lead); lead **audits post-hoc** (source-verify + test-evidence, revert only on failed audit), not pre-gate. COORDINATION field 4 + a new contract field updated. **Two assessments dispatched via Rally Flow** (read-only): Codex → rally-cli simplification; Claude workflow `wor9rkkhp` → Rally Flow simplification + NavGator orphan/architecture scan + API/tool/instruction-surface scan. **Feature named "Rally Flow."** Folded Claude #2's cross-host five-gaps input → PROPOSALS #11 (presence/liveness + readable goal/intent + autonomy envelope; rest overlap #2/#7/#10/worktree-rule). NOTE: that input arrived via **user relay, not rally** — Claude #2 wrote it to its worktree-local board (invisible); reinforces "input = rally fact, not worktree board edit."
- **2026-05-29 (sync 21)** — **B9 ✅ (`ea6aee9`, lead-verified)** — Codex closed the lib.rs residuals (advanced-flag/envelope/capture/wait_for_resolution all in the diff + tests; clippy/fmt/test green). **The entire B1–B9 backlog is now closed + source-verified.** Codex agents idle. Only the **L4 merge to `main`** remains (gates B10–B12 / B2 / L5). The whole rally-cli hardening (every audit HIGH + the L3/L3b/L2 fixes) is done on main; the dynamic-workflows feature (L1+L6) is done; doctrine + proposals are written.
- **2026-05-29 (sync 20)** — **Board nit fixed** (Codex flag seq314, correct protocol — it can't edit the board, surfaced via fact): stale "build-loop Claude-only" wording in sync-19 corrected to install-dependent. **B3 closed clean** (Codex audit seq298: L4 branch adds no new parser surface). **Roster-visibility call posted**: rally sees only 2 Claude ids (`claude_code:lead`, `claude_code`/L4); other Opus terminals are invisible until they `rally enter` with a distinct scoped id. Stale handoffs (seq268 superseded, seq306 ack'd, seq314 fixed) resolved.
- **2026-05-29 (sync 19)** — **Lead handoff snapshot added** (top of board — resume point for a fresh lead; dogfoods the "resume from board + rally" claim) given the long session. **Backlog built from dogfood-3**: the 10 ranked proposals grouped into **B10–B12** (pickable, gated on L4 merge since they touch check/store/next.rs) — B10 = canonical-path matching (rank-1 linchpin) → first to delegate to Codex on merge. **Build-loop is host-relative + install-dependent** (NOT Claude-only — that earlier claim was corrected: present where cached, e.g. Codex build-loop@0.13.3 seq306; absent in another Codex runtime seq283; else use the host-equivalent verify-loop). Instruction-contract standard (11 fields) is working: Codex now executes contracts without standing-by.
- **2026-05-29 (sync 18)** — **Dogfood 3 (review→automate) Claude half landed** → `docs/RALLY-AUTOMATION-PROPOSALS.md`: 9 ranked charter-safe rally capabilities to automate the lead's manual coordinations (linchpin: canonical path matching; quick wins ranks 1-3; core-human residuals enumerated). 10 Sonnet reviewers + Opus synthesis (`wkp5iue2u`); **Codex half delegated, PENDING**. Integration was **delegated** (not hand-merged) per the lead-delegates-integration rule. **L4 verified-done on branch** (Claude #2, seq232) → **B3 unblocked** (→ codex); B2/L5 unblock on L4 **merge to main**. **Codex re-routed** with clear lanes (B9 + B3 + .gitignore) after standing-by — itself a live proof of the proactive-self-routing gap (proposal rank 7). Fixed: route.mjs/`./route` export risk + MODEL-TIERS A/B count (both Codex-flagged, lead-lane).
- **2026-05-29 (sync 17)** — **A/B (Sonnet vs Opus) complete** (`wjh0kr38l`) → verdict **MARGINAL**: Sonnet=recall (but ~1 FP/batch), Opus=precision (0 FP, caught the top bug). Calibration in `MODEL-TIERS.md`: assessment runs at **Executing + verification gate**; Frontier reserved for correctness-critical. The A/B doubled as a real audit — **verified-real new findings routed to owners**: store.rs unconsumed_artifacts truncation (HIGH) + next.rs path_boost/release_claims/untargeted-handoff → **Claude #2/L4 to fold**; lib.rs residuals → **B9** (codex). **False positive explicitly discarded** (normalize_components L1155 — source inverts it; the verify-gate caught it). Model-tier taxonomy: `MODEL-TIERS.md` (frontier/executing/fast, per-host mapping).
- **2026-05-29 (sync 16)** — **L6 route.mjs LANDED** (`e1589dd`): native pi deterministic-routing (parallel/pipeline/budget) ported host-neutral, **improved not blindly cloned** — onError/abort failure-visibility (a failed item must be distinguishable for resume) + generic budget units; 35 module tests. **Shared-tree collision handled**: the primary checkout had been switched to `claude2/l4-audit-folds` (Claude #2's L4, uncommitted store.rs), so my route commit landed on the wrong branch → cherry-picked to main (`e1589dd`). **Systems fix**: primary stays on `main`; branch work uses worktrees (COORDINATION.md rule + decision); lead now operates from `~/dev/git-folder/arp-lead`. Handoff sent to Claude #2 to move L4 into its own worktree + restore primary to main.
- **2026-05-29 (sync 15)** — **Coordination doctrine captured** → `dynamic-workflows/COORDINATION.md` (the joining-guide for frontier agents): two modes (deterministic subagent routing *lifted from pi* for short defined tasks vs frontier-agent coordination), the rules (first-agent-is-lead · proactive engagement: backlog→no-regrets→check-lead · de-conflict on canonical paths, cross-lane ripples → lead · checkpoint to rally for resume), and **rally facilitates ≠ coordinates** (context-blind substrate can't dictate; agents decide, rally de-conflicts). This is the doctrine this very session dogfooded. Lightweight by design.
- **2026-05-29 (sync 14)** — **B8 ✅ (`4e1a329`)** — bounded_i64 now errors on out-of-range (verified); ambiguous-flag guardrails rejected. **ENTIRE BACKLOG CLOSED** (B1, B4–B8). Audit→fix→verify loop fully complete across the 4-agent roster. **Only L4/PR46 (Claude #2's lane) remains**, with L5/B2/B3 gated on it. 0 claims, 0 blockers. Lead now monitoring-only until L4 lands.
- **2026-05-29 (sync 13)** — **B1 ✅** (`2ca7d33`, lead-verified): `.codex-plugin/skills/dynamic-workflows` → symlink to canonical `dynamic-workflows/skills/codex` (single source; `../../PROTOCOL.md` resolves through the symlink — verified, not a dangling ref). **B8 in flight** (Codex, cli/output). Remaining open: L4 (Claude #2 PR46), L5/B2/B3 (gated on L4). Backlog B1/B4–B7 all closed.
- **2026-05-29 (sync 12)** — **ALL audit HIGHs closed** (lead source-verified): B5 ✅ (`e7b87aa`+`3bdc040` — shell_quote NUL-strip per my no-signature-change ruling; session-cleanup propagated), B7 ✅ (`c72328f`+`d39aab7` — corrupt index preserved not clobbered). B4/B6 already done. The B5 ruling avoided a `next.rs`/L4 collision. Codex idle post-cleanup → **B8 (last backlog, cli/output) delegated to Codex**. 0 claims/blockers. Module 13/13 green.
- **2026-05-29 (sync 11)** — **B4 ✅ (`12468f9`) + B6 ✅ (`6ec1042`) landed** (lead source-verified: check.rs allow=!stop + missing-arg errors; backends shell_words→Result + NUL test). **Lead caught dropped work**: B5 (lib.rs shell_quote still `->String`) and B7 (discovery:95 `unwrap_or_default` unchanged) had claims released *without landing* — **re-opened + re-delegated** to codex. Source-check > artifact-trust. **L6 planned + building**: pi-spawn-logic + rally-checkpointing = durable/resumable fan-out (fixes pi's long-running gap); `core/workstream-status.mjs` resume helper in progress.
- **2026-05-29 (sync 10)** — **Collision deconflicted (live)**: `claude`(#2) claimed `src/lib.rs` (seq81) while Codex held `crates/rally-cli/src/lib.rs` (B4 plumbing + B5) — same file, missed by auto-detect (path forms differed → the B7/B8 scope bug). Lead ruling: **lib.rs → Codex**; Claude #2 released and redirected to its clean lane next.rs+store.rs. Added **policy 6 (canonical claim paths)**. Concurrent: B4 (check.rs+lib.rs plumbing) + B6 (backends.rs) in flight by Codex; B5 lib.rs Codex; Claude #2 unblocking onto next/store.rs.
- **2026-05-29 (sync 9)** — **Roster engaged (2 Claude + 2 Codex)** per user. Rebalanced to use Claude #2 and remove file collisions: **Claude #2 → L4/PR46** (next.rs+store.rs) and folds B7-store + B8-next audit findings into PR46. **B7 re-scoped to discovery.rs only** (Codex); **B8 re-scoped to cli.rs+output.rs**; **B4 stays Codex** (check.rs, claimed seq73). No two agents now own the same file. Codex: B4/B5/B6/B7(discovery). Roster section added above.
- **2026-05-29 (sync 8)** — **Lead decision**: HIGH audit fixes **B5/B6/B7 delegated to codex** (parallel — file-disjoint: lib.rs / backends.rs / discovery+store.rs), not left idle. **B8 (med/low) stays open** for free pickup. Codex now owns B1+B4+B5+B6+B7 (it manages its own fan-out). B7 shares store.rs with pending L4 — coordinate.
- **2026-05-29 (sync 7)** — **Scale dogfood RESULT**: workflow `wnll6qruh` (10 Sonnet read-only agents, 2 scales) returned **34 findings (8 high, 14 med, 12 low)**, 0 failures, ~3.3 min. Full triage: `docs/audit-2026-05-29-rally-cli.md`. Scale A (3 agents/big files) density 5.3/agent vs Scale B (7 agents) 2.6/agent. Backlog **B4–B8** opened (file-scoped, MECE). **B4 (check.rs gate bypass — security) delegated to codex**; B5–B8 open for free pickup. Flagged the high cluster via rally `risk`. NOTE: B4/B7/B8 share files with the pending L4/PR46 lane — coordinate if concurrent.
- **2026-05-29 (sync 6)** — **Scale dogfood launched**: read-only audit workflow `wnll6qruh` — 10 Sonnet `Explore` agents across two scales (A=3 core files, B=7 per-file), structured findings → backlog (results pending). Descriptor lint-validated. **B1 delegated to codex** (answering coordinator check-in seq49; mostly `.codex-plugin` surface). Codex landed a 3rd small fix — "artifact review completion loop" (`5a6726e`, pushed). Board-ownership rule (policy 5) held: Codex didn't touch this file.
- **2026-05-29 (sync 5)** — **Delegation + backlog protocol added** (per user): lead may execute / delegate via rally handoff / backlog for free pickup, with a documented pickup protocol gated on deps. Backlog seeded B1 (open), B2/B3 (blocked on L4). **Board-ownership rule added** (policy 5: lead-only edits; others post rally facts) after conflict markers were briefly committed and resolved at `3ae7ec8`.
- **2026-05-29 (sync 4)** — **L3b CLOSED**: Codex fixed room projection so `resolve --ref <risk>` removes the risk from
  `current_risks`. Validation: `cargo test --all` 23/23 user journeys, `cargo clippy --all-targets -D warnings`, fmt,
  and diff-check clean in the room-state worktree.
- **2026-05-29 (sync 3)** — `codex:dynwf-coordinator` completed read-only integration review (seq 28) and defers the next BUILD lane to lead (seq 30). New lane **L3b** opened: codex:dynamic-scale-01 fixing resolved-risk leakage in the room projection (claim seq 27). Lead pushed local `main` (incl. L3 fix + scale hardening) to origin to preserve stop points for follow-on dogfooding. **Next build lane is open and awaiting user direction** (candidates: L1 discovery wiring, or L5 seam once L4 lands).
- **2026-05-29 (sync 2)** — Lead identity scoped to `claude_code:lead` (room decision); bare `claude_code` reserved for L4/PR46 terminal.
  **L3 CLOSED**: codex:dynamic-scale-01 fixed the bpaf panic (`9332915`,`b056855`); lead-verified `rally run --help` exits 0. Local `main` b056855 ahead of origin 237d067 (push pending). `codex:dynwf-coordinator` acknowledged lead (seq 26). Dogfood loop confirmed: risk seq13 → claim seq18 → fix → artifact seq21.
- **2026-05-29** — Lead established (`claude_code`); board created. L1 landed + tests 7/7 after L2 merges (no regression).
  L3 claimed by `codex:dynamic-scale-01`; bpaf `--help` still panics. L4 not yet on `main`. Risk seq 13 (bpaf) → claim seq 18.

> To update: edit the table + append a dated line here, then `rally say` the status change and commit with the
> `docs(orchestration): sync board` prefix. Keep this file and `rally room` in agreement.
