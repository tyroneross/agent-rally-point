{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:agent-rally:command:say:v1",
  "title": "Rally Say Command",
  "type": "object",
  "required": ["ok", "product", "command", "schema", "data"],
  "properties": {
    "ok": { "const": true },
    "product": { "const": "rally" },
    "command": { "const": "say" },
    "schema": { "const": "agent-rally.command.say.v1" },
    "data": {
      "type": "object",
      "required": ["fact", "room"],
      "properties": {
        "fact": {
          "type": "object",
          "required": ["schema", "event_id", "thread_id", "created_at", "tool", "kind", "subject", "scope", "evidence"]
        },
        "room": { "type": "object" }
      }
    }
  }
}
