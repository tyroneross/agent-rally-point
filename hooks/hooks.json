{
  "$comment": "Agent Rally Point plugin hooks — auto-loaded by Claude Code at this standard path (do NOT also declare a 'hooks' key in .claude-plugin/plugin.json; see reference_plugin_hooks_autoload.md). The hook self-gates on .rally/ presence (silent no-op if absent) and fail-opens (never blocks). Advisory by default; export RALLY_HOOK_STRICT=1 to hard-block on high-severity coordination signals. Minimal surface: SessionStart for room awareness, PreToolUse for deconflict before edits.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "timeout": 5000,
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/rally-coordination-hook.sh start claude_code"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "timeout": 10000,
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/rally-coordination-hook.sh before-write claude_code"
          }
        ]
      }
    ]
  }
}
