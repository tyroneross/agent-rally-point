{
  "$comment": "GENERATED from config/host-integrations.json. sessionStart injects additional_context; beforeSubmitPrompt runs idle side effects with no model channel; preToolUse matches Write|StrReplace|Delete|EditNotebook and can inject agent_message. See docs/AUTO-COORDINATION-HOOKS.md.",
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "RALLY_HOOK_SOURCE=project bash \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/hooks/rally-coordination-hook.sh\" start cursor",
        "timeout": 5
      }
    ],
    "beforeSubmitPrompt": [
      {
        "command": "RALLY_HOOK_SOURCE=project bash \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/hooks/rally-coordination-hook.sh\" idle cursor",
        "timeout": 5
      }
    ],
    "preToolUse": [
      {
        "command": "RALLY_HOOK_SOURCE=project bash \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/hooks/rally-coordination-hook.sh\" before-write cursor",
        "timeout": 10,
        "matcher": "Write|Edit|StrReplace|Delete|EditNotebook"
      }
    ],
    "stop": [
      {
        "command": "RALLY_HOOK_SOURCE=project bash \"$(git rev-parse --show-toplevel 2>/dev/null || pwd)/hooks/rally-coordination-hook.sh\" after-write cursor",
        "timeout": 5
      }
    ]
  }
}
