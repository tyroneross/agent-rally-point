[workspace]

[package]
name = "factstr-sqlite"
version = "0.5.2"
edition = "2024"
rust-version = "1.85"
description = "Embedded SQLite store for FACTSTR with append, query, streams, and durable streams."
license = "MIT OR Apache-2.0"
repository = "https://github.com/ricofritzsche/factstr.git"
homepage = "https://factstr.com"
readme = "README.md"
keywords = ["eventstore", "eventsourcing", "sqlite", "embedded", "facts"]
categories = ["database-implementations"]

[lib]
path = "src/lib.rs"

[dependencies]
factstr = "0.5.2"
serde_json = "1"
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio-rustls"] }
time = { version = "0.3", features = ["formatting", "parsing"] }
tokio = { version = "1", features = ["rt"] }
